{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Modifier la réservation {% endblock %}

{% block body %}

<main>
      <form action="{{path ('editReservationConfirmation') }}/{{ reservation.id }}" method="POST">
            {% set datetime_object = reservation.date %}
                Dr {{ reservation.doctor.lastName }} - {{ reservation.center.name }} - {{ datetime_object|date("d/m/Y H:i") }} 
                <div class="form-group row">
                    <label 
                    for="comments" 
                    class="col-4 col-form-label">
                    </label> 
                <div >
                <textarea id="comments" name="comments" cols="40" rows="5" class="form-control">{% if reservation.comments != null %}{{reservation.comments}}{% endif %}</textarea>
                </div>
                <div class="form-group row mt-2">
                    <div class=" col-8">
                        <button type="submit" class="btn btn-primary">Confirmer</button>
                </div>
        </div>
        </div> 
        </form>

</main>

{% endblock %}